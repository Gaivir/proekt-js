{{#each this}}
<div class="evt-card">
    <img src="{{images.1.url}}" alt="photo" class="evt-img" width="200" height="227">
    <h6 class="evt-title">{{name}}</h6>
    <p class="evt-date">{{dates.start.localDate}}</p>
    <p class="evt-location">{{_embedded.venues.0.name}}</p>

    {{!-- <a href="" class="evt-location">{{}}</a> --}}
</div>
{{/each}}